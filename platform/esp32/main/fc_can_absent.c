// Описание отсутствующей шины к контроллерам мотора (ТЗ v0.6A §13).
//
// Отдельный файл ровно на одну строку — чтобы было очевидно, что никакого
// кода CAN в прошивке нет: ни драйвера, ни настройки пинов, ни очереди.
// Проверяется таблицей символов в tools/esp32_smoke.sh.

#include "fc_platform.h"

const char *fc_can_backend_name(void) {
    return "unavailable (TWAI не собран, трансивер не подключён)";
}
