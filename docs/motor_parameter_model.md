# Параметры мотора: что известно, что прочитано и что подтверждено

Документ этапа v0.7C. **Мотор физически отключён, детекция не проводилась.**
Ни одно значение отсюда не участвует в выходе на мотор — выхода не
существует.

Модель в исполняемом виде: `compat/motor/fc_motor_model.{h,c}`, проверки —
`tests/safety/test_motor_prep.c`.

---

## 1. Зачем понадобилась степень доверия

Проект уже дважды получал правдоподобное, но неверное число.

Сначала заглушки FloatCore (`flux = 0.0045`, `poles = 30`) дали расчётный
коэффициент **120 А/градус** — величину, при которой доска насыщает ток на
наклоне в одну двенадцатую градуса. Аномалия была замечена на v0.6E.

Потом реальная конфигурация ESC (`flux = 0.1`, `poles = 14`) дала
**11.6 А/градус** — величину правдоподобную. Но правдоподобие не равно
истине: обе цифры прочитаны из настроек, которые кто-то когда-то ввёл, и ни
одна не измерена на моторе, который будет подключён.

Поэтому у каждого параметра здесь есть не только значение, но и
происхождение, и степень доверия.

## 2. Три степени доверия

| Степень | Что означает |
|---|---|
| `НЕ ПОДТВЕРЖДЕНО` | значение известно, но ничем физически не подкреплено |
| `прочитано из ESC` | прочитано при нас и сверено двумя независимыми путями — доверие к **каналу**, не к числу |
| `подтверждено на моторе` | измерено или проверено на физически подключённом моторе |

Сейчас в состоянии «подтверждено на моторе» — **ноль параметров из десяти**.

Разница между второй и третьей степенью — суть документа. На v0.7B
конфигурация была прочитана через CAN и **побайтово** сверена с независимым
чтением по USB: `FW_VERSION` и `APPCONF` совпали целиком, в `MCCONF`
разошлись только смещения датчиков, которые ESC меряет при каждом включении.
Это доказывает, что канал не врёт. Это ничего не говорит о том, к какому
мотору относятся числа.

## 3. Три категории параметров

| Категория | Смысл | Кто задаёт |
|---|---|---|
| Измеряется детекцией | FOC-детекция выдаёт результат | процедура, не человек |
| Введено человеком | детекция это не проверяет | человек |
| Предел безопасности | не свойство мотора, а наше решение | мы, и детекция не должна это трогать |

Разделение практическое: после детекции **первая** категория обязана быть
перечитана, **третья** обязана остаться нетронутой. Тест проверяет, что все
параметры первой категории помечены к обновлению.

## 4. Таблица

Значения прочитаны через CAN на v0.7B, обе половины Dual FSESC.

| Параметр | 118 | 100 | Категория | Доверие | Нужен Refloat | Можно ли опираться до детекции |
|---|---|---|---|---|---|---|
| `foc_motor_r` | 0.3196 Ом | 0.3162 Ом | детекция | прочитано | нет | **нет** |
| `foc_motor_l` | 730 мкГн | 866 мкГн | детекция | прочитано | нет | **нет** |
| `foc_motor_flux_linkage` | 0.1 Вб | 0.1 Вб | детекция | прочитано | **да** | **нет** |
| `si_motor_poles` | 14 | 14 | человек | **НЕ ПОДТВЕРЖДЕНО** | **да** | **нет** |
| `l_current_max` | 25 А | 25 А | предел | прочитано | да | да |
| `l_current_min` | −5 А | −5 А | предел | прочитано | да | да |
| `l_in_current_max` | 15 А | 15 А | предел | прочитано | нет | да |
| `si_battery_cells` | 3 | 3 | человек | **НЕ ПОДТВЕРЖДЕНО** | **да** | **нет** |
| `si_wheel_diameter` | 0.083 м | 0.083 м | человек | **НЕ ПОДТВЕРЖДЕНО** | нет | нет |
| `si_gear_ratio` | 3.0 | 3.0 | человек | **НЕ ПОДТВЕРЖДЕНО** | нет | нет |

`fc_motor_model_ready_for_output()` возвращает **false** и будет возвращать
false, пока хоть один нужный параметр не получит степень «подтверждено на
моторе». Умолчание — «не готова»: модель закрыта, пока не доказано обратное.

## 5. Число полюсов — почему 14 не считается истиной

**Детекция FOC число полюсов не определяет.** Она измеряет сопротивление,
индуктивность и потокосцепление; полюса в её выход не входят. Значит `14`
пришло от человека и проверено ничем.

Где оно используется:

* механическая скорость и обороты — `ERPM / (polepairs)`, то есть скорость,
  одометр, отображение в интерфейсе;
* Refloat читает его через `CFG_PARAM_si_motor_poles`;
* на само управление FOC (коммутацию) число полюсов **не влияет**: FOC
  работает в электрических углах, которые даёт датчик или наблюдатель.

Вывод: ошибка в числе полюсов не приведёт к неверной коммутации, но приведёт
к неверной скорости — а от скорости зависят пороги отката Refloat.

**Определить без вращения нельзя.** Единственный надёжный способ — покрутить
вывешенное колесо на известной механической скорости и сравнить с ERPM. Это
делается на этапе первого прокрута, а не раньше.

Типовые мотор-колёса моноколёс имеют 30 полюсов (15 пар). Значение 14 — это
7 пар, что характерно скорее для outrunner-мотора скейта. Это **подозрение, а
не вывод**: подтвердить обязано измерение.

## 6. Потокосцепление — почему 0.1 подозрительно

`foc_motor_flux_linkage = 0.1000` **ровно**, на обеих половинах, при том что
сопротивление и индуктивность у них различаются (0.3196 против 0.3162 Ом,
730 против 866 мкГн).

Это внутреннее противоречие. Если детекция делалась раздельно — а различие R
и L говорит именно об этом, — то и flux вышел бы разным. Одинаковое круглое
число на фоне разных измеренных — признак того, что его **ввели руками**, а
детекция его не касалась.

Правдоподобие величины: 0.1 Вб характерно для крупного низкооборотного
мотор-колеса; старая заглушка 0.0045 Вб — для маленького быстрого outrunner.
То есть 0.1 ближе к истине, чем то, что стояло раньше. Но «ближе к истине»
недостаточно, чтобы на этом считать ток.

### Пересчёт коэффициента — только как анализ

С реальными параметрами Refloat дал бы примерно **11.6 А на градус наклона**
против 120 А/градус на заглушках (`can_bringup.md`, раздел 10). При
`si_motor_poles = 30` вместо 14 тот же расчёт даёт 5.4 А/градус.

Разброс между 5.4 и 11.6 — ровно двукратный и целиком объясняется
неподтверждённым числом полюсов. **Ни то, ни другое значение не должно
попасть в путь вывода до детекции**, и в текущей сборке не может: заглушки
`fc_vesc_if.c` намеренно не менялись на этом этапе (см. §8).

## 7. Параметры, явно относящиеся к другому аппарату

`si_wheel_diameter = 0.083 м` (83 мм) и `si_gear_ratio = 3.0`.

У моноколеса диаметр порядка 0.28 м и прямой привод, то есть передаточное
отношение 1.0. Восемьдесят три миллиметра с редукцией втрое — это
конфигурация чего-то другого, скорее всего оставшаяся от прежнего
использования контроллера.

На управление это не влияет (только на расчёт скорости и одометра), но
хорошо иллюстрирует общий вывод документа: **в этой конфигурации есть
параметры, которые заведомо не относятся к нашему аппарату.** Из чего следует,
что и остальные надо перепроверить, а не принять на веру.

## 8. Почему заглушки FloatCore не приведены к прочитанным значениям

`platform/esp32/main/fc_vesc_if.c` по-прежнему отдаёт Refloat
`si_motor_poles = 30`, `flux = 0.0045`, `si_battery_cells = 15`.

Соблазн подставить туда прочитанные 14 / 0.1 / 10 велик и на первый взгляд
безобиден: мотора нет, хуже не станет. Но подставить неподтверждённое число
означает сделать его невидимым — дальше оно будет выглядеть согласованным и
проверять его никто не станет. Пока значение заведомо-неверное, вопрос
остаётся открытым и заметным.

Поэтому все предложенные изменения собраны в change set
(`first_motor_bringup_plan.md` §7) и ждут отдельного решения, а модель здесь
хранит и прочитанное значение, и степень доверия к нему.
