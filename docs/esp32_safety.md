# Безопасность прошивки FloatCore

Документ отвечает на один вопрос: **почему эта прошивка физически не может
сформировать команду мотору или кадр CAN через реализованный программный
путь.** Не «мы не будем», а «нечем».

Область действия — лабораторный профиль `FLOATCORE_LAB_SAFE`, этапы
v0.5–v0.6x. Как только появится реальный передатчик, документ обязан быть
переписан целиком. Список того, что для этого надо сделать, — в разделе
«Requirements before enabling motor output» в конце.

---

## 1. Единственный путь от Refloat к мотору

SDK VESC содержит двенадцать функций, способных что-либо подать на мотор.
Список получен вычиткой `refloat-upstream/vesc_pkg_lib/vesc_c_if.h:436-447,
476, 653`, а не по памяти:

```
mc_set_current            mc_set_duty            mc_set_handbrake
mc_set_brake_current      mc_set_duty_noramp     mc_set_handbrake_rel
mc_set_current_rel        mc_set_pid_speed       mc_release_motor
mc_set_brake_current_rel  mc_set_pid_pos         foc_play_tone
```

`foc_play_tone` в списке не по ошибке: звук на VESC издаётся подачей
напряжения на обмотки, то есть это тоже выход на мотор.

**Все двенадцать указателей заполнены и ведут в одну функцию.** Заполнены
именно все, а не только используемые Refloat: оставленный NULL — это место,
куда однажды впишут прямой вызов мимо политики.

```
Refloat → VESC_IF->mc_set_*()
        → fc_motor_gate_request(kind, value, now)
              ├─ 1. значение конечно и в пределах лимита?
              ├─ 2. супервизор не в FAULT?
              ├─ 3. состояние разрешает выход?      ← в LAB_SAFE: никогда
              └─ 4. backend существует?             ← в LAB_SAFE: нет как кода
        ← возврат. Всё.
```

Дальше кода нет. Устройство политики и счётчики — `safety_supervisor.md`.

---

## 2. Пять независимых причин, почему выхода нет

Каждая из них по отдельности достаточна.

### 2.1 В прошивке нет драйвера CAN

Ни один файл не включает `driver/twai.h` и не вызывает функций `twai_*`.
Компоновщик это подтверждает — в `.elf` **ноль** символов TWAI:

```
$ xtensa-esp32-elf-nm build/floatcore_esp32.elf | grep -i twai
(пусто)
```

Проверка автоматизирована и входит в `tools/esp32_smoke.sh` (шаг 4): наличие
хотя бы одного символа TWAI объявлено stop condition и валит прогон. Там же
проверяется отсутствие символов `fc_motor_gate_set_backend`,
`manual_set_current`, `manual_set_duty`, `motor_can_send`,
`real_motor_backend`, `safety_bypass` и присутствие `refloat_init`,
`fc_motor_gate_request`, `fc_supervisor_poll`, `icm20948_read`.

Единственные совпадения по подстроке `can_` в прошивке —
`can_engage` (функция состояния самого Refloat, к шине отношения не имеет),
`fc_can_backend_name` (наша строка для консоли) и `s_flash_op_can_start`
(внутреннее имя ESP-IDF).

### 2.2 Пины не сконфигурированы

Ни один GPIO не переводится в режим CAN: нет ни `gpio_set_direction`, ни
подключения к матрице периферии. `VESC_IF->io_set_mode()` и
`VESC_IF->io_write()` реализованы как заглушки, возвращающие `false`, — даже
пищалка не заводится.

### 2.3 Трансивера нет физически

К плате не подключён ни CAN-трансивер, ни VESC, ни мотор (ТЗ §19). Даже если
бы драйвер существовал и пины были настроены, на шине не было бы уровней.

### 2.4 Backend физической отправки не существует как код

В профиле `FLOATCORE_LAB_SAFE` макрос `FC_MOTOR_BACKEND_AVAILABLE` равен нулю,
и функция `fc_motor_gate_set_backend()` **не объявляется и не определяется**.
Зарегистрировать backend нечем.

Это проверяется не на слово: отдельная компиляция файла с вызовом этой функции
падает с ошибкой «call to undeclared function», а в таблице символов её нет.

### 2.5 Разрешение на выход — компилируемая константа

`fc_supervisor_motor_output_permitted()` в лабораторном профиле возвращает
литеральный `false`. Компилятор видит это и выкидывает недостижимые ветки.
Ни одно состояние супервизора, включая `READY`, разрешения не даёт;
`ARMED`/`RUNNING` недостижимы, потому что переходов в них не написано.

**Снять блокировку одним флагом или параметром нельзя.** Нужно сменить профиль
сборки, написать код передачи, включить компонент драйвера, настроить пины,
подключить трансивер. Это отдельный этап с отдельной ревизией.

---

## 3. Что видно в трассе

Блокировка не молчаливая: каждый запрос считается и классифицируется. Команда
`safety` в любой момент:

```
SUPERVISOR   state=DISARMED faults=none(0x00000000) latched=0x00000000
  profile    LAB_SAFE
  imu(hw)    OK age=2.3 ms ok=31434 errors=0 stuck=0 stale=0
  loop       500.0 Hz p99=2480 us max=3166 us late=1266 missed=0
  footpad    NONE
  cfg_write  allowed (принято=1 отклонено=0 коммитов=1, худший 5061 мкс)
  motor      requested=30681 rejected(disarmed=30681 fault=0 invalid=0)
             allowed=0 sent=0 backend=none (blocked)
  can        unavailable (TWAI не собран, трансивер не подключён)
```

`physically_sent` инкрементируется исключительно backend-ом физической
отправки. Его в этой сборке нет, поэтому счётчик обязан всегда быть нулём —
это проверяется в каждом host-тесте и в smoke-скрипте.

Соотношение «одна команда на семпл IMU» сходится: 30 681 запрос за 62 с при
контуре 500 Гц.

## 4. Footpad: почему доска считается неактивированной

Решение Refloat принимает так (`refloat-upstream/src/footpad_sensor.c:31`):

```c
bool adc1_on = config->fault_adc1 == 0.0f || adc1 > config->fault_adc1;
```

Датчик считается нажатым, если напряжение **выше** порога. Пороги по
умолчанию — 2.00 В (`settings.xml`, `fault_adc1`/`fault_adc2`). Платформа
отдаёт **0.00 В**, что ниже любого допустимого ненулевого порога из диапазона
0.01…3.30 В — это перебором проверено host-тестом.

Значение −1.0 В («пина нет на железе») намеренно не используется: оно тоже
ниже порога, но означало бы «канала не существует», а мы сообщаем «канал есть
и он отпущен».

**Отдельный опасный случай, который значением ADC не лечится.** Если порог
выставлен в 0, первая ветка условия делает зону нажатой **всегда**, при любом
напряжении. Поэтому фактическое состояние проверяется не по ADC, а по самому
Refloat: прошивка после старта печатает

```
[floatcore] footpad: NONE, adc 0.00/0.00 V, пороги 2.00/2.00 V -> DISENGAGED (ok)
```

и логирует ошибку, если состояние окажется не `FS_NONE`. Это прямая проверка
stop condition из ТЗ («mock ADC приводит Refloat в engaged state»).

---

## 5. Mock IMU: почему покой — это покой

Контракт выведен из upstream, а не назначен (подробности —
`platform/esp32/main/fc_imu_mock.c`):

| Величина | Значение | Обоснование |
|---|---|---|
| `imu_get_roll/pitch/yaw` | 0 рад | `imu.c:36-40` оборачивает в `rad2deg()` |
| `acc` в callback | (0, 0, +1) g | `balance_filter.c:100` при единичном кватернионе даёт направление гравитации +Z; любой другой вектор заставил бы Mahony доворачивать доску |
| `gyro` | (0, 0, 0) рад/с | `balance_filter.c:114-125` интегрирует `q += 0.5*q*g*dt` |
| кватернион | (1, 0, 0, 0) | покой |
| `dt` | реальный интервал `esp_timer` | `frequency_tracker`/`latency_tracker` считают частоту по нему |

Результат на плате: `pitch/roll 0.000 / 0.000 deg` и `imu 500.0 Hz` по
счётчикам самого Refloat.

`imu_startup_done()` возвращает `false` первые 1.5 секунды — как время
калибровки гироскопа на VESC. Пока он `false`, Refloat остаётся в
`STATE_STARTUP`, то есть в безопасном состоянии по умолчанию.

---

## 6. Радио выключено

`CONFIG_BT_ENABLED=n`, `CONFIG_ESP_WIFI_ENABLED=n` в `sdkconfig.defaults`.
Ни Wi-Fi, ни BLE не инициализируются, provisioning не делается, транспорт
VESC Tool на плату не переносился (ТЗ §18). Ядро 0 остаётся свободным, контур
целиком на ядре 1.

---

## 7. Инвентарь диагностических команд

Консоль (`platform/esp32/main/fc_console.c`) не имеет доступа ни к одной
функции, способной что-либо запросить у мотора. Классификация выражена
структурой файла, а не только таблицей ниже.

### SAFE_READONLY — компилируются всегда

| Команда | Что делает |
|---|---|
| `status` | состояние системы, супервизора и Refloat |
| `supervisor` | состояние, причины отказов, все входы |
| `imu` | конфигурация датчика, статистика, последний сырой семпл |
| `timing` | периоды и время исполнения всех задач |
| `timing-hist` | гистограмма периодов контура |
| `tasks` | остаток стека по задачам |
| `heap` | куча |
| `config` | конфигурация, политика записи, статистика |
| `safety` | сводка безопасности и счётчики Motor Gate |
| `imu-cal-show` | текущая калибровка, её статус и ход измерения |
| `can` | сводка по шине CAN: счётчики, состояние контроллера, идентификаторы |
| `can-frames` | последние 32 принятых кадра |
| `i2cscan` | опрос адресов 0x08…0x77 и чтение `WHO_AM_I` у ответивших |
| `help` | список команд |

`i2cscan` относится сюда с оговоркой, которую стоит сделать явно: чтобы
прочитать `WHO_AM_I`, команда выбирает нулевой банк записью в `BANK_SEL`. Это
переключение окна регистров, которое драйвер выполняет и при каждом обычном
чтении, а не изменение конфигурации датчика. Если шина не поднята, команда
поднимает её — ради этого случая она и добавлена.

### STATE_CHANGING — только при `FC_LAB_DIAGNOSTICS`

Меняют состояние системы, но физически безопасны.

| Команда | Что делает | Почему безопасна |
|---|---|---|
| `ready` | `DISARMED → READY` | `READY` не разрешает тягу ни в одном профиле |
| `disarm` | `READY → DISARMED` | снятие готовности всегда безопасно |
| `fault-clear` | снять залипший отказ | не проходит, пока причина активна |
| `persist` | записать косметический параметр яркости LED | подчиняется политике записи; лента не подключена |
| `timing-reset` | обнулить статистику | только счётчики |
| `restart` | `esp_restart()` | перезагрузка возвращает систему в `BOOT` |
| `imu_stress [сек] [кГц] [порог_сброса]` | стресс-тест шины I2C: замещает штатную задачу чтения и читает ICM-20948 без пауз (~1640 Гц). Необязательные аргументы — частота шины и число отказов подряд до сброса шины (0 — не сбрасывать) | только шина датчика; Refloat всё это время работает от mock-IMU, Motor Gate не затрагивается. Задача пиннится на ядро housekeeping, контур не трогает — см. `fc_imu_stress.c` |
| `imu_stress-stop` | досрочно завершить тест | снимает флаг; штатное чтение восстанавливается автоматически |
| `imu_stress-log` | показать предысторию первого отказа | только печать сохранённого кольца |
| `imu-cal-detect [yaw]` | измерить калибровку ориентации и смещений | ничего не записывает во flash; на движущейся доске не завершается |
| `imu-cal-save` | записать измеренную калибровку | подчиняется политике записи: только в `DISARMED`, коммит делает задача хранилища |
| `imu-cal-clear` | стереть калибровку, вернуть единичную | то же; после стирания READY недостижим |
| `can-reset` | обнулить статистику CAN | только счётчики |
| `can-stop` / `can-start` | остановить и запустить приём CAN | останавливается только ПРИЁМ: передачи в этой сборке не существует как кода. Нужна, чтобы приписывать изменения тайминга именно приёму, а не предполагать |

### LAB_DIAGNOSTICS — только при `FC_LAB_DIAGNOSTICS`

Намеренно ломают подсистему, чтобы доказать, что защита срабатывает.
Требуют подтверждения прямо в имени команды.

| Команда | Что делает | Что доказывает |
|---|---|---|
| `wdtest-confirm` | контур не отмечается 8 с | супервизор ловит `LOOP_STALL` за 1.5 с, TWDT — за 8.6 с |
| `crashtest-confirm` | разыменование нулевого указателя | panic печатает причину, регистры, backtrace и останавливает CPU |
| `imu-fail-confirm` | 500 ошибок чтения подряд | `READ_ERROR` → `FAULT (IMU_UNHEALTHY)` |
| `imu-freeze-confirm` | 500 повторов одного семпла | `STUCK` → `FAULT (IMU_UNHEALTHY)` |
| `imu-stop-confirm` | остановить задачу чтения | нужна для сравнительных прогонов тайминга |
| `imu-start` | запустить её заново | — |

### SAFETY_BYPASS и MOTOR_OUTPUT

**Таких команд не существует.** Ни одной команды, способной ослабить политику
безопасности или подать что-либо на мотор, в прошивке нет. Проверяется
поиском символов `manual_set_current`, `manual_set_duty`, `motor_can_send`,
`safety_bypass` в `.elf`.

### Что будет в профиле мотора

В `FLOATCORE_MOTOR_CAPABLE` макрос `FC_LAB_DIAGNOSTICS` равен нулю, поэтому
две последние категории **не компилируются вовсе**. Это не флаг, который можно
переключить во время работы: соответствующих функций в двоичном файле нет.

## 8. Requirements before enabling motor output

Исчерпывающий список того, что должно быть сделано **до** того, как хоть одна
команда сможет уйти на мотор. Пока хотя бы один пункт открыт, профиль
`FLOATCORE_MOTOR_CAPABLE` собирать и прошивать нельзя.

### Канонический список (актуален на v0.8A)

Разделы ниже — подробности и история; этот список — сводка того, что открыто
**сейчас**. Закрытые пункты в него не входят, они помечены зачёркиванием в
подробностях.

| # | Блокер | Где подробности |
|---|---|---|
| 1 | Термистор мотора: на вход ESC не приходит ничего, защита по перегреву не работает на ОБЕИХ половинах | `second_motor_bringup.md` §9 |
| 2 | Измерить `foc_motor_r` на достаточном токе — на пределе 5 А метод даёт ±20 % на обоих моторах | `second_motor_bringup.md` §4 |
| 3 | Измерить диаметр колеса и подтвердить прямой привод (сейчас 0.083 м и 3.0) | G, пункт 47 |
| 4 | Определить рабочие пределы тока и вернуть прокрутные 5 / −3 / 5 / −1 А на ОБЕИХ половинах | G, пункт 45 |
| 5 | Определить рабочую политику `l_in_current_min` на ОБЕИХ половинах — при нуле торможения нет | G, пункт 45 |
| 6 | Определить политику при потере ОДНОЙ половины ESC | D, пункт 44 |
| 7 | Пересчитать коэффициент контура от измеренных λ и полюсов (двухмоторная машина: момент вдвое) | G, пункт 48 |
| 8 | Проверить `app_to_use` непосредственно перед испытанием с человеком | G, пункт 35 |
| 9 | Финальная квалификация I2C на окончательном монтаже — **до замкнутой балансировки** | `imu_motor_interference.md` §7 |
| 9a | Платформа не сообщает политике IMU о залипании шины и неудаче переинициализации: они входят в модель, но подаются явными нулями | `imu_health_policy.md` §11 |
| 9b | Компенсация мёртвого времени на выходе в прошивке VESC отсутствует — отсюда мёртвая зона ±0.38 А. Решение (компенсация, частота ШИМ или учёт в контуре) — отдельный этап | `vesc_low_current_root_cause.md` §8 |
| 9c | Расхождение скважности половин 11 % не установлено: половины опрашиваются последовательно, нужен одновременный отсчёт | `low_current_response.md` §6 |
| 10 | Калибровка смещений акселерометра — до замкнутой балансировки | G, пункт 47 |
| 11 | Пороги неподвижности IMU — до замкнутой балансировки | G, пункт 47 |
| 12 | Полоса ФНЧ акселерометра 246 Гц — до замкнутой балансировки | G, пункт 47 |

Список отсортирован по порядку: пункты 1-3 требуют работы с железом, 4-8 —
решений по политике, 9-12 закрываются только перед замкнутой балансировкой.

**Закрыто на v0.9C:** политика здоровья IMU переделана — одиночный
транзиент больше не равен потере датчика, появилось восстановимое
состояние HOLD, пороги выведены из одной временной модели
(`imu_health_policy.md`). Причина мёртвой зоны найдена в коде и в
физике: некомпенсированное мёртвое время инвертора, предсказание
0.381 А против измеренных 0.30-0.40 (`vesc_low_current_root_cause.md`).

**Закрыто на v0.9B:** расхождение показаний приборов объяснено кодом и
воспроизведено на железе — в отказ ведут два пути, и порог 40 мс
участвует только в одном (`imu_time_model.md`). «Последнее время IMU»
сведено к одной канонической шкале. Мёртвая зона характеризована:
трогание с места исключено, порог одинаков на вращающемся колесе
(`low_current_response.md`).

**Закрыто на v0.9A:** транспорт моторных команд существует и проверен на
вывешенном колесе; единственность пути, матрица отказов, частичная
передача, сторожевой таймер и установившийся прогон —
`experimental_motor_backend.md`. Лабораторный профиль остаётся неспособным
передать моторную команду.

**Закрыто на v0.8C:** версия наводки от моторов на шину I2C ослаблена
измерением — 0 отказов на 491 404 транзакциях под моторной нагрузкой,
масштаб дефекта v0.6C исключён. Механическая хрупкость жгута не
воспроизводится (`imu_motor_interference.md`).

**Закрыто на v0.8B:** параметры второй половины подтверждены физически —
полюса 30, λ 19.961 мВб, таблица Холла проверена, направление совпадает
с мотором A (`second_motor_bringup.md`). Прежний пункт 2 списка снят.

**Закрыто на v0.7C:** блокирующий диагностический UART (пункт 38).
**Закрыто на v0.7D:** пропуски дедлайна `refloat_thd` (пункт 42) — причина
оказалась в механизме сна, а не в приоритетах. Пятиминутный прогон: пропусков
ноль на всех каналах.
**Закрыто на v0.7D.1:** модель батареи (пункт 43) и политика таймаута ESC
(пункт 44) — записаны в железо и пережили полный цикл питания.
**Закрыто на v0.8A:** число полюсов и параметры мотора половины 118 (пункты
31, 37), направление вращения (пункт 26), пределы прокрута (пункт 45
применён), соответствие разъёма половине 118 — всё измерением. Сторожевой
таймер 50 мс подтверждён физически.

Перемерить терминацию придётся заново на **финальном жгуте** доски: пункт 34
закрыт для текущего стенда, а не для проводки, которой ещё нет.

Пункт 8 (проверить `app_to_use` перед испытанием с мотором) с v0.7B стало
возможно выполнять **не разбирая стенд**: конфигурация читается через CAN и
сверена с эталоном по USB побайтово. Сам пункт от этого не закрывается —
проверять его надо непосредственно перед подключением мотора, а не заранее.

Сверх этого остаются открытыми пункты, появившиеся раньше и не относящиеся к
одной конкретной подсистеме: A (1, 2, 4–6), B (7–9), C (10–14), D (15, 17, 18),
E (19, 20).

**Чего в списке нет намеренно.** Единицы `imu_get_gyro()` закрыты на v0.6D.1
прямым доказательством по `vedderb/bldc` (пункт 27). Опасение «два
балансировочных контроллера одновременно» снято на v0.7A: на VESC нет ни
Refloat, ни LispBM-скрипта, ни QML-приложения, а `app_to_use = UART`
(`can_bringup.md` §8). Остался только пункт 9 — перепроверить это перед
испытанием, а не исправлять несуществующее.

### A. Датчик и ориентация

1. **Финальный механический монтаж IMU.** Сейчас модуль закреплён временно;
   гравитация распределена между двумя осями.
2. **Привязка осей датчика к осям доски** с физическим обоснованием каждой
   перестановки и инверсии — не подбором «пока UI не выглядит правильно».
   *Измерено на этапе v0.6B (`imu_orientation_mapping.md`), но получено на
   прежнем монтаже: после переборки железа требует повторного подтверждения.*
3. ~~**Передача реальных данных в Refloat** через `VESC_IF->imu_*` и проверка
   pitch/roll ручным наклоном.~~ *Выполнено на v0.6D
   (`real_imu_refloat_integration.md`): Refloat работает от физического
   ICM-20948, знаки подтверждены шестью движениями и знаком запрошенного тока.*
4. **Калибровка**: смещения акселерометра, нулевое смещение гироскопа, ноль
   pitch/roll доски. *Частично: на v0.6D реализована стартовая калибровка
   смещения гироскопа (только на неподвижной доске, с проверкой правдоподобия
   и блокировкой READY до завершения) — расхождение двух источников углов
   упало с 2.7° до 0.2°. НЕ сделано: смещения акселерометра и ноль монтажа
   доски. Наклон покоя +6.5° больше, чем `startup_pitch_tolerance` = 4°, —
   в этом положении доска не входит в RUNNING.*
5. **Аппаратный DRDY** вместо программного опроса, чтобы ритм задавал датчик.
6. **Детекция потери семплов** по счётчику датчика, а не только по возрасту.

### B. Реальное время

7. **Слияние задачи чтения датчика и контура в одну.** Сейчас две независимые
   задачи с одинаковым периодом конкурируют и дают 3 % опозданий
   (`realtime_timing.md` §3, гипотеза 4).
8. **Перемер времени исполнения контура на реальных данных**: сейчас
   `imu_ref_callback` работает на mock-нулях.
9. **Проверка гипотезы о вымывании кэша** (рост времени исполнения с 93 до
   179 мкс при активном I2C).
38. ~~**Диагностический UART блокирует контур.**~~ **ЗАКРЫТО** на v0.7C,
    подробности — `docs/motor_safety_architecture.md` §6.

    Найденная причина оказалась шире загрузочного баннера. Синхронная печать
    была достижима **из самого контура**: задача `fc_imu_rt` при серии
    отказов чтения вызывает `icm20948_init()`, а тот печатает до
    четырнадцати строк подряд; и `VESC_IF->printf()` зовёт сам Refloat из
    `refloat_thd` — код upstream, который мы не правим. При 115200 бод это
    сотни миллисекунд ровно в тот момент, когда шина уже сбоит.

    Сделано: кольцевой журнал `compat/log/fc_log.{h,c}` с отдельной
    низкоприоритетной задачей печати на ядре housekeeping. Производитель
    форматирует строку в слот фиксированного размера и уходит; при
    переполнении запись отбрасывается и **считается**, а не ждёт. Все
    realtime-достижимые места переведены на него.

    Фаза загрузки выделена явным событием `fc_boot_phase_complete()`:
    счётчики тайминга разделены на две фазы, а не обнулены.

    Измерено на плате, окно 73 с от сброса:

    ```
    фаза загрузки:        control missed 0, refloat_thd missed 0…1
    установившийся режим: control missed 0, max период 2864 мкс
                          (было 4381 мкс до изменения)
    журнал:               потерь 0, обрезаний 0, пик кольца 3 из 48
    ```

    Остаточный пункт — не UART, см. пункт 42.

39. ~~**`si_battery_cells` — `NEEDS REVIEW`.**~~ **ЗАМЕНЁН пунктом 43**, где
    разобраны все три места, а не одно. Исходная формулировка ниже.
    Сейчас 3 на обеих половинах, при
    этом отсечки выставлены 34/31 В, что соответствует батарее 10S. Значение не
    менялось и меняться автоматически не должно. Перед `MOTOR_CAPABLE`:
    определить реальную батарею, проверить число ячеек и пределы напряжения,
    убедиться, что расчёты Refloat и интерфейса используют верное число.

40. **Профиль `ACTIVE_DIAG` (v0.7B): передача есть, но только на чтение.**
    С этого этапа в сборке существует передача в CAN, и это меняет модель
    безопасности — но не ослабляет её.

    Что разрешено: пять запросов (`PING`, `FW_VERSION`, `GET_VALUES`,
    `GET_MCCONF`, `GET_APPCONF`), два типа пакета CAN из 69, семь номеров
    COMM. Всё остальное **отсутствует как код**: обобщённой отправки нет ни в
    одном профиле, моторного намерения нет в перечислении, имена моторных
    обёрток отравлены, единственная функция, вызывающая `twai_transmit()`, —
    статическая. Четыре негативных теста компиляции и проверка таблицы
    символов в smoke это подтверждают на каждой сборке.

    Что осталось прежним: `FC_CAN_TX_AVAILABLE = 0`,
    `FC_MOTOR_BACKEND_AVAILABLE = 0`, Motor Gate `allowed=0 sent=0
    backend=none`, `MOTOR_CAPABLE` отсутствует. Сборка `ACTIVE_DIAG` вместе с
    моторным транспортом или backend-ом вывода запрещена директивой `#error`.

    Подробности и доказательства — `docs/can_active_diag.md`.
41. **Здоровье узлов CAN к разрешающей логике не подключено — намеренно.**
    Модель `compat/can/fc_can_health.c` считает доступность обеих половин
    FSESC, но входы Supervisor `esc_a_alive`, `esc_b_alive`, `can_fresh` из
    неё не заполняются.

    Причина: правило «оба VESC живы — значит можно» создало бы второй путь к
    моторной команде помимо Motor Gate, и проверять пришлось бы два места
    вместо одного. Отсутствие VESC обязано только запрещать. Когда эти входы
    начнут заполняться, они должны попасть в Supervisor как запрещающее
    условие, а не как разрешающее — это требование к будущему этапу, а не
    рекомендация.

42. ~~**`refloat_thd` изредка пропускает дедлайн из-за `aux_thd`.**~~
    **ЗАКРЫТО на v0.7D, но объяснение было НЕВЕРНЫМ** — подробности в
    `docs/refloat_scheduling_semantics.md`.

    Что было записано на v0.7C: оба потока Refloat идут с одинаковым
    приоритетом, и `aux_thd` своим исполнением задерживает главный.

    Что оказалось на самом деле: `aux_thd` **сам понижает свой приоритет**
    первым же действием (`refloat-upstream/src/main.c:1112`,
    `VESC_IF->thread_set_priority(-1)`), и мы это реализуем
    (`fc_vesc_if.c:253`). Проверка на живой плате: Main 12, Aux 10. Вытеснить
    главный поток вспомогательный не может в принципе, и вся гипотеза
    строилась на непроверенной посылке.

    Настоящая причина — механизм сна. `esp_timer` исполняет обработчик в
    ЗАДАЧЕ, закреплённой за ядром 0, тогда как потоки Refloat живут на ядре 1:
    каждое пробуждение было межъядерным уведомлением. Измерено: главный поток
    просил 1801 мкс, спал 2078, задержка до 964 мкс. Поскольку Refloat
    вычитает из следующего сна время итерации, задержка не компенсировалась —
    контур шёл на 400 Гц вместо 500.

    Заменено на `xTaskDelayUntil` по абсолютной отметке: пробуждение в
    обработчике системного тика, на своём ядре, без посредника — то же, что
    делает `chThdSleepMicroseconds` на ChibiOS. Результат: период ровно
    2000.0 мкс, число итераций главного потока совпало с числом итераций
    контура, пропусков дедлайна ноль на пятиминутном прогоне.

    Отдельно испробован и отвергнут аппаратный `gptimer` с пробуждением из
    прерывания: измерение показало задержку 1662 мкс и 5718 пропусков, то есть
    заметно хуже. Изменение откачено; запись оставлена, чтобы к этому варианту
    не возвращались наугад.

43. ~~**Модель батареи противоречива.**~~ **ЗАКРЫТО на v0.7D.1.** Батарея
    **10S Li-ion**. Подробности — `docs/battery_safety_model.md`, порядок
    применения и побайтовые доказательства — `docs/esc_config_checkpoint.md`.

    Применено: число ячеек в FloatCore 15 → 10 (v0.7D); `si_battery_cells`
    3 → 10 на обеих половинах ESC; пороги отката Refloat
    `tiltback_lv` 3.0 → **3.6 В/яч**, `tiltback_hv` 4.3 → **4.0 В/яч**.
    Всё пережило полный цикл питания.

    Оба порога выведены из исходников Refloat, а не подобраны. Предложенные на
    v0.7D 3.5 и 4.1 **оказались неверны**:

    * 3.5 В/яч не спасает при большом токе. Единственная ветка, срабатывающая
      при любом токе, — `vdelta > 2` (`main.c:621`), значит порог обязан быть
      не меньше `cut_start + 2 = 36 В`. При 35 В и токе 20 А откат не
      сработал бы, хотя ESC уже срезает ток.
    * 4.1 В/яч опаздывает. Немедленная ветка требует `порог + 1`
      (`main.c:532`), то есть 42.0 В — где рекуперация уже срезана полностью.
      Порог обязан быть не больше `regen_cut_start − 1 = 40.5 В`.

    `fc_battery_model_valid()` на применённых значениях возвращает true.

44. ~~**`timeout_msec = 1000` не соответствует политике.**~~ **ЗАКРЫТО на
    v0.7D.1.** Записано **50 мс** на обеих половинах, пережило полный цикл
    питания. `timeout_brake_current` оставлен **0.0** — выбег, а не
    торможение, и для балансировочной доски это верно.

    Худшее удержание последней команды при полном отказе FloatCore
    (перезагрузка ESP32, когда ни один наш барьер не работает) стало
    50 + 10 мс округления = **60 мс** вместо секунды.

    Физически нулевая тяга не проверялась — мотор отключён; проверен
    конфигурационный контракт, `docs/esc_config_checkpoint.md` §7.

45. **Пределы тока для первого прокрута не выставлены.** Сейчас на ESC
    25 А моторного, −5 А тормозного, 15 А входного — это рабочие значения, а
    не значения для первого включения мотора, который ещё ни разу не крутился
    под нашим управлением.

    Предложено на время прокрута: 5 А / −3 А / 5 А, плюс тот же предел в
    Motor Gate независимо. Многослойная политика и обоснование — в
    `docs/motor_safety_architecture.md` §4, change set — в
    `docs/first_motor_bringup_plan.md` §7. Значения не менялись.

    Отдельно к обсуждению: асимметрия 25/−5 в рабочей конфигурации означает,
    что доска разгоняется впятеро сильнее, чем тормозит. Решение это или
    наследство — неизвестно, и до первой езды вопрос надо закрыть.

    **`l_in_current_min = 0.0` разобран на v0.7D.1 и оказался не тем, чем
    казался** (`docs/motor_parameter_model.md` §9). Ограничение накладывается
    не на ток батареи, а на `iq`, с делением на `mod_q`
    (`mcpwm_foc.c:3564-3567`). При нуле нижняя граница равна `0/mod_q`, то
    есть **ровно нулю**: тормозного момента не возникает вовсе, пока колесо
    вращается. На v0.7D это было записано как «энергия не попадёт в батарею,
    значит безопасно» — безопасно, но потому, что торможения нет, а это
    делает замкнутую балансировку невозможной.

    Решение: **−1 А** для первого прокрута, вместе с остальными пределами.
    Для езды значение будет другим и определяется на реальной машине.

46. **Финальная квалификация I2C: требуется перед балансировкой, не перед
    первым прокрутом.** Классификация по ТЗ v0.7D §17.

    Обоснование от последствия отказа, а не от удобства. Во время первого
    прокрута вывешенного колеса мотором управляет VESC Tool по USB, а FloatCore
    только наблюдает: контур замкнут не через нас. Отказ I2C в этот момент
    означает потерю телеметрии IMU, но **не потерю управления** — управлять
    нечем.

    С момента, когда контур замыкается через FloatCore, всё меняется: пропажа
    IMU становится пропажей ориентации, а значит и способности удерживать
    равновесие. Поэтому граница проходит здесь:

    * **до первого прокрута вывешенного колеса** — не обязательна;
    * **до первого замкнутого контура балансировки** — обязательна;
    * до теста с человеком на доске — тем более обязательна.

    Supervisor при этом не ослабляется ни на одном из этапов: пороги health
    остаются прежними, и отказ I2C по-прежнему запрещает выход на мотор.
47. **Оставшиеся вопросы IMU — по этапам.** Классификация по ТЗ v0.7D §18.

    | Вопрос | Этап | Почему так |
    |---|---|---|
    | Калибровка смещений акселерометра | **B** — до замкнутой балансировки | смещение акселерометра смещает вертикаль, а на вывешенном колесе вертикаль ни на что не влияет |
    | Пороги неподвижности при Detect | **B** — до замкнутой балансировки | они охраняют старт контура; пока контура нет, охранять нечего |
    | Полоса ФНЧ 246 Гц при Найквисте 250 | **B** — до замкнутой балансировки | наложение спектра портит оценку ориентации; на прокрут без обратной связи не влияет |

    Ни один из трёх не блокирует первый прокрут вывешенного колеса. Все три
    блокируют замкнутую балансировку. Закрывать их «заодно» с моторным этапом
    не нужно и вредно: это разные проверки с разными условиями.

48. **Первое подключение мотора выполнено (v0.8A): половина 118.**
    Подробности — `docs/first_motor_bringup_plan.md` раздел «Что получилось».

    Соответствие разъёма установлено **измерением, не осмотром**: вращение
    колеса от руки изменило тахометр половины 118 на 88 шагов, а половины 100
    — на ноль. Ни одной моторной команды при этом не отправлялось.

    Детекция FOC выполнена штатным VESC Tool по USB. FloatCore на всём этапе
    только слушал шину и выполнял read-only запросы: белый список не
    расширялся, `allowed=0 sent=0 backend=none`, `MOTOR_CAPABLE` отсутствует,
    ни одного кадра `SET_*` на шине за 5370 с наблюдения.

    Направление подтверждено: **положительная команда → верх колеса к носу →
    вперёд**. Проверено десятью повторами в одну сторону и одним в обратную.
    `m_invert_direction = 0`, инверсия не нужна.

    Половина 100 не тронута: ни одного изменившегося байта за весь этап.
49. **Сторожевой таймер 50 мс подтверждён физически.** На v0.7D.1 проверялся
    только конфигурационный контракт — мотор был отключён.

    Наблюдение получилось побочным продуктом проверки направления: одиночная
    команда скважности из VESC Tool даёт рывок колеса и выбег. Ровно 50 мс
    момента, дальше поток Timeout на ESC видит молчание и снимает тягу
    (`set_brake_current(0.0)`). Удержание кнопки не помогает, потому что
    удерживается кнопка, а не поток команд.

    Следствие для будущего профиля MOTOR_CAPABLE: **команды придётся слать
    непрерывно с частотой контура.** Спроектированная политика — 500 Гц
    команд, свой таймаут 20 мс, таймаут ESC 50 мс — получает физическое
    подтверждение: при 2 мс между командами запас двадцатипятикратный.
50. **Термистор мотора не работает — защита по перегреву мотора
    неработоспособна.** `temp_motor` показывает около −88 °C, то есть ESC
    считает мотор переохлаждённым и по перегреву никогда не отключит.

    **Вывод про «датчик есть, но тип не тот» на v0.8B опровергнут счётом.**
    Показание −85 °C при β3380 и R₀ = 10 кОм означает вычисленные 7.6 МОм,
    то есть разрыв; а PT1000 или KTY84 (около 1 кОм) дали бы по той же
    формуле **+101 °C**, а не минус восемьдесят. Колебания показаний —
    шум разомкнутого входа: вблизи насыщения АЦП один младший разряд
    превращается логарифмом в десятки градусов. На вход ESC не приходит
    ничего, и одинаково на обеих половинах. Разбор —
    `second_motor_bringup.md` §9.

    На детекцию и прокрут не влияет: за такое время мотор не нагревается. Но
    до замкнутой балансировки, а тем более до езды, разобраться обязательно —
    иначе единственной защитой мотора от перегрева останется ограничение
    тока.

### D. Шина и мотор

15. **Драйвер TWAI** и протокол VESC поверх него. *Приём реализован на v0.7A
    (`FLOATCORE_CAN_PASSIVE`, listen-only). На v0.7B добавлен профиль
    `FLOATCORE_CAN_ACTIVE_DIAG`: normal mode и передача пяти read-only
    запросов, см. пункт 40. Транспорт команд мотору по-прежнему отсутствует
    как код — это отдельный этап.*
16. **Аппаратный watchdog команды на стороне ESC** (`timeout_msec` около
    100 мс), проверенный отключением передачи.
17. **Ограничение тока на уровне ESC**, а не только в нашей политике.
18. **Стендовая проверка** с колёсами в воздухе и ограничением тока, до
    любого человека на доске.

### F. Шина I2C — квалификация на финальном железе

Ни один пункт этого раздела не выполнен. Стенд, на котором получены текущие
измерения, временный: длина жгута, его прокладка и механическая фиксация
модуля ещё не определены, а значит результаты нельзя переносить на финальную
конструкцию.

```text
Before enabling motor output:

- IMU must be installed in final mechanical configuration.
- Final I2C harness length/routing must be used.
- SDA/SCL pull-up configuration must be documented.
- I2C stress test must be repeated on final hardware.
- Mechanical stress test must complete with:
    failed_reads == 0
    timeouts == 0
    recoveries == 0
    bus_resets == 0
```

21. **Финальная механическая конфигурация IMU** — не временное крепление.
22. **Финальные длина и прокладка жгута I2C.**
23. **Конфигурация подтяжек SDA/SCL задокументирована.** Текущее измеренное
    значение — 10 кОм на модуле, см. `i2c_stress_test.md`; для финального
    жгута его придётся подтвердить заново.
24. **Стресс-тест шины повторён на финальном железе** командой `imu_stress`.
25. **Механический стресс-тест пройден** с `failed_reads == 0`,
    `timeouts == 0`, `recoveries == 0`, `bus_resets == 0` на протяжении не
    менее двух минут воздействия. Ослаблять этот критерий ради зелёного
    результата нельзя: он и существует для того, чтобы поймать именно то, что
    уже ловил.

### E. Сборка

19. **Удаление категорий `LAB_DIAGNOSTICS` и `STATE_CHANGING`** из профиля
    мотора — уже обеспечено `FC_LAB_DIAGNOSTICS`, но требует проверки в
    собранном двоичном файле.
20. **Расширение символьных проверок** под новый профиль: там TWAI будет
    присутствовать, поэтому проверять придётся не отсутствие, а корректность
    единственного пути.

## 9. Как проверить утверждения этого документа

```bash
. ~/esp/esp-idf/export.sh
tools/esp32_smoke.sh    # символьные проверки профиля LAB_SAFE и маркеры загрузки
make test               # 120 протокольных + 11 сценариев Refloat + 29 платформы + 61 безопасности
python3 tools/esp32_serial.py --seconds 12 --send safety --send supervisor
```

Отдельно проверяется, что backend мотора нельзя зарегистрировать: компиляция
файла с вызовом `fc_motor_gate_set_backend()` в профиле LAB_SAFE падает,
потому что функции не существует.
