build/
.DS_Store
__pycache__/
*.pyc

# ESP-IDF: артефакты сборки и сгенерированная конфигурация проекта.
# sdkconfig.defaults версионируется, сам sdkconfig — нет.
platform/esp32/build/
# Сборка второго профиля CAN для сверки символов (v0.7B).
platform/esp32/build_passive/
platform/esp32/sdkconfig
platform/esp32/sdkconfig.old
platform/esp32/managed_components/
dependencies.lock

# Каталог сборки экспериментального профиля (v0.9A): отдельный, чтобы
# лабораторный образ оставался нетронутым и пригодным для сверки символов.
platform/esp32/build_exp/

# Все каталоги сборок ESP-IDF, включая диагностические варианты (build_exp,
# build_probes, build_isr0, build_ab_* ...). На v0.9I шесть таких каталогов
# попали в коммит, потому что правила были перечислены поимённо.
platform/esp32/build_*/
