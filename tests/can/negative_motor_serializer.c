// Негативный тест компиляции (ТЗ v0.9A §22, §23, инвариант I2).
//
// Этот файл ОБЯЗАН НЕ КОМПИЛИРОВАТЬСЯ в профиле LAB_SAFE.
//
// Проверяется главное свойство v0.9A: появление настоящего транспорта в
// экспериментальном профиле не сделало его доступным в лабораторном. Граница
// проходит по реализации, а не по вызову: тело fc_vesc_can_motor.c целиком
// закрыто профилем, и в лабораторной сборке это пустая единица трансляции —
// ни одного символа, а не «функция, которую не зовут».
//
// Если файл собрался — это провал теста, а не успех.
#include "../../compat/can/fc_vesc_can_motor.h"

void floatcore_attempt_motor_serialize(void) {
    FcMotorFrame f;
    fc_vesc_can_motor_build_current(118, 5.0f, &f);
}
