// Негативный тест компиляции (ТЗ v0.7B §6, §19).
//
// Этот файл ОБЯЗАН НЕ КОМПИЛИРОВАТЬСЯ в пассивном профиле. Появление
// диагностической передачи не должно означать, что она доступна везде:
// пассивный профиль обязан остаться профилем, из которого нельзя послать
// вообще ничего.
//
// Весь API диагностики закрыт `#if FC_CAN_DIAG_TX_AVAILABLE`, поэтому в
// пассивной сборке не существует ни типа FcCanDiagFrame, ни перечисления
// запросов, ни функции сборки кадра.
#include "../../compat/can/fc_can_diag.h"

void floatcore_attempt_diag_in_passive(void) {
    FcCanDiagFrame f;
    fc_can_diag_build(FC_CAN_DIAG_PING, FC_CAN_SELF_ID, 118, &f);
}
